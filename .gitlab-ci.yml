image: node:latest

stages:
  - deploy
  
build-and-deploy:
  stage: deploy
  image: ruby:latest
  script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=bot-wbb --api-key=$HEROKU_API_KEY
  only:
    - master